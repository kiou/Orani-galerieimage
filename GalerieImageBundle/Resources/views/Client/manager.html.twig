{% extends "GlobalBundle:Client/Layout:layout.html.twig" %}

{% block metaTitle %}Meta title{% endblock %}
{% block metaDescription %}Meta description{% endblock %}

{% block contentitle %}
    <h1>Les galeries d'images</h1>
{% endblock %}

{% block content %}
    <div class="recherche">
        <form action="{{ path('client_galerieimage_manager') }}" method="post">

            <select name="categorie">
                <option value="">Choisir une catégorie</option>
                {% for categorie in categories %}
                    {% if recherches.categorie == categorie.id %}
                        <option value="{{ categorie.id }}" selected>{{ categorie.nom }}</option>
                    {% else %}
                        <option value="{{ categorie.id }}">{{ categorie.nom }}</option>
                    {% endif %}
                {% endfor %}
            </select>

            <button type="submit" name="addRecherche"><i class="fa fa-search"></i></button>
        </form>
    </div>

    <div class="galeriesImages">

        {% for galerie in pagination %}
            <div class="galerieImage">
                <div class="galerieImageTitre">
                    <h3><a href="{{ path('client_galerieimage_view',{'slug': galerie.slug, 'id': galerie.id})}}">{{ galerie.titre|truncate(30) }}</a></h3>
                </div>

                <div class="galerieImageImage">
                    {% if galerie.image %}
                        <img src="{{ asset('img/galerie/miniature/' ~ galerie.image) }}">
                    {% endif %}
                </div>

                <div class="galerieImageDate">
                    <p>{{ galerie.created|date("d/m/Y") }}</p>
                </div>

                <div class="galerieImageResume">
                    <p>{{ galerie.resume|truncate(120) }}</p>
                </div>

                <div class="galerieImageLien">
                    <a href="{{ path('client_galerieimage_view',{'slug': galerie.slug, 'id': galerie.id})}}"><i class="fa fa-angle-right"></i> Voir plus</a>
                </div>
            </div>
        {% else %}
            <p>Aucune galerie d'image</p>
        {% endfor %}

        <div class="clear"></div>

    </div>

    {# display navigation #}
    <div class="navigation">
        {{ knp_pagination_render(pagination) }}
    </div>
{% endblock %}